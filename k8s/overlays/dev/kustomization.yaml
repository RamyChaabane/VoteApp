apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Include base components
resources:
  - ../../base
  - backend-hpa.yaml
  - frontend-hpa.yaml

patches:
  - path: backend-replicas-patch.yaml
